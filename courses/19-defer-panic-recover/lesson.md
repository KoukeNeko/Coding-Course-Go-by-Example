# 延遲執行與中斷
- **Defer**: 會將函式的執行推遲到外層函式返回之前。多個 `defer` 會以後進先出 (LIFO) 的順序執行。常用於關閉檔案或解鎖。
- **Panic**: 遇到無法恢復的錯誤時使用，會立即中斷當前函式的執行。
- **Recover**: 只能在 `defer` 函式中使用，用來捕捉並處理 `panic`，防止程式崩潰。

## 任務：
在 `main` 函式的第一行加上一個 `defer`，讓程式在結束時印出 "Cleanup complete"。