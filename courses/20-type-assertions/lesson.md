# 空介面與型別斷言
- **空介面 `interface{}` (或 `any`)**: 可以裝載任何型別的值。
- **型別斷言**: 用來將介面變數轉換回原本的具體型別。語法為 `value, ok := i.(Type)`。如果斷言失敗，`ok` 會是 false 而不會引發 panic。

## 任務：
嘗試將空介面變數 `i` 斷言為 `string` 型別，並印出結果。如果斷言失敗，則印出 "Not a string"。